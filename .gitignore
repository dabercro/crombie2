# Don't need resulting test files 
test/*/*
build/*

# Don't save output in bin directory
bin/*

# Don't need .deps
*/.deps

# Don't save generated doxygen pages
docs/*/*

# Don't save any generated files for now
*m4*
depcomp
install-sh
missing

# You better know this one
*~
